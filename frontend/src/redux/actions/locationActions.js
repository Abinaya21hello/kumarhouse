export const setSelectedCountry = (countryCode) => ({
    type: 'SET_SELECTED_COUNTRY',
    payload: countryCode,
  });
  
  export const setSelectedState = (stateCode) => ({
    type: 'SET_SELECTED_STATE',
    payload: stateCode,
  });
  